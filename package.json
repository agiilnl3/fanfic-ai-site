{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "lint": "prettier --check \"artifacts/api-server/src/lib/sentry.ts\" \"artifacts/api-server/src/lib/featureFlags.ts\" \"artifacts/api-server/src/routes/flags.ts\" \"artifacts/api-server/tests/**/*.ts\" \"artifacts/api-server/vitest.config.ts\" \"artifacts/fanfic-ai/src/hooks/use-flag.ts\" \"artifacts/fanfic-ai/src/lib/sentry.ts\" \"lib/db/src/schema/feature-flags.ts\""
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "stripe": "20.0.0",
    "stripe-replit-sync": "1.0.0"
  }
}
